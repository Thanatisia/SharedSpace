# Nginx - CLI utility

## Services
+ service name: nginx

## Documentation

### Synopsis/Syntax
```console
nginx {options}
```

### Parameters
+ -s : Stop the nginx server instance
+ -t : Test the nginx configuration file

### Usage
- Test nginx configuration
    ```console
    nginx -t
    ```
